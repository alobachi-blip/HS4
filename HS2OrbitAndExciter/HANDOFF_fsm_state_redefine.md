# 交接：H 流程狀態機重定義

> **已過時。** 請改讀：[`HANDOFF_fsm_contract_review.md`](HANDOFF_fsm_contract_review.md)  
> 主計畫：`C:\Users\jason\.cursor\plans\fsm_狀態圖確認_bd3a5656.plan.md`

---

## 你是誰、要做什麼

Repo：`D:\HS4`，外掛 **HS2OrbitAndExciter**。  
延續「H 流程狀態機簡潔化」：**狀態驅動、一格一出口、換段只認選池**；與原版女方主導／自動挑姿隔離。

**主計畫（唯一真相來源）**  
`C:\Users\jason\.cursor\plans\fsm_狀態圖確認_bd3a5656.plan.md`

舊計畫／舊交接可能過時：  
`C:\Users\jason\.cursor\plans\窺視_n_l_回_idle_70e3914d.plan.md`  
`HS2OrbitAndExciter/HANDOFF_fsm_autoplay_plan.md`  
→ 以 **`fsm_狀態圖確認_bd3a5656.plan.md` 的「0. 總則（已定稿）」** 為準。

語言：對使用者用 **繁體中文**；**不自創縮詞**。  
問問題格式：狀況 → 選項差在哪 → 建議 → 請回「第 N 題選 X」。  
過程節奏標「過程」；會改 FSM 切換／出口的必須先警告「這會改邏輯」，預設禁止。

---

## 產品目標

> 開 Ctrl+Shift+O 後，幾乎不介入也能享受整場：開幹→高潮→換姿→窺視播完再換。  
> 可选手動（換角／換衣／換視角／L／跳出）；不介入不卡死。

方法論：告別「到處等 N 秒」時間驅動；改為清晰狀態＋條件出口。

---

## 狀態圖（拓樸鎖死）

```text
選池
  ├─ 動作線姿 → 閒置（對話旗＋開幹）→ 動作橋段 → 高潮後閒置 → 選池
  └─ 窺視 → In…Out_Loop → 播完／N／L → 選池
L＝使用者版選池
換角／換衣／相機／刺青等＝圖外正交
```

禁止：跨狀態寫出口；第二套換段；實作順便改圖。

---

## 0. 總則八點（已全部定稿）

1. **未開 Ctrl+Shift+O**＝外掛完全不介入（流程＋熱鍵都關）。  
2. **與原版女方主導／第 8 顆／自動挑姿隔離**；換段只認選池。禁止寫 `initiative`／`isAutoActionChange`、禁止 `GetAutoAnimation` 當換段。  
3. **時間＝B**：禁「等 N 秒」當換段或高潮後回同姿循環；讀檔／淡入出／對話可等；橋段內高潮補洞少用純計時。  
4. **進 H 第一姿**：不先選池；直接閒置開幹；高潮後才第一次選池。  
5. **自慰＝動作線**；純播出只留窺視；允許無插入；拆舊債（場所自慰勿當長欣賞）。  
6. **不自動換角、不自動換衣**；G／H／J 僅手動。  
7. **高潮後安靜不擋**「高潮後閒置→選池」；語音／特效用事件，不用等秒數擋換段。  
8. **狀態權威**：第一女動畫狀態名＋是否窺視；不以計時器當權威。  
9. **過程 vs 邏輯**：節奏可調（如閒置落地後約 1 秒）；會改 FSM 切換／出口的提案必須先警告，預設禁止。

---

## 重定義排序（做到哪）

| 序 | 項目 | 狀態 |
|----|------|------|
| 0 | 總則 | **已定** |
| 1 | **選池** | **已定**（混池；去重 B1；換角不清；空池放寬再試） |
| 2 | 閒置 | **已定**（含 N 立刻開幹） |
| 3a～3c | 動作橋段 | **已定**（感度每階段≈10s；3loop+衝刺≈40s；單loop≈20s；語音可變化） |
| 4 | 高潮後閒置→選池 | **草案；待高潮後題 A～D** |
| 5 | 窺視段 | 待定 |
| 6a～6c | 輸入閘／跳出／L·N | 待定 |
| 7～10 | 假滾輪收窄／關逾時／迴轉／落地政策 | 待定 |
| 11～23 | 正交（相機／換角／衣／刺青／胸噴腹／語音…） | 待定 |

每一項只填：進場／職責／自動出口／手動／禁止／現況落差。

---

## 現況程式最大落差（實作時必拆）

- 推 `initiative`／`isAutoActionChange`；`GetAutoAnimation`；AfterProc 補旗標  
- 高潮後閒置 → 同姿回循環（應改選池）  
- `TryForceStartSex` 直跳循環、跳過插入  
- 長欣賞名單綁自慰＋窺視  
- 尚無統一「選池」API  
- `CanAcceptHotkey` 高潮中擋 G（換角契約後要放行）

關鍵檔：`OrbitBehaviorHub.cs`、`OrbitPoseDirector.cs`、`OrbitPoseLandedPolicy.cs`、`OrbitManualDirector.cs`、`OrbitHelpers.cs`  
原版參考：`dll_decompiled/Sonyu.cs`、`Masturbation.cs`、`Peeping.cs`、`HScene.cs`、`HSceneSprite.cs`（第 8 顆＝女方主導類）

---

## 溝通約定（使用者已要求）

- 問題必須講清楚，歡迎一次多題。  
- 不要丢抽象縮寫表。  
- `LeaveItToYou`＝原版第 8 類姿勢／女方主導模式，不是外掛發明；外掛曾借用其旗標 → **已定隔離**。

---

## 建議下一任第一步

1. 讀本交接＋主計畫「0. 總則（已定稿）」。  
2. 起草 **1. 選池** 六欄草案，並列出需拍板的清楚選擇題（池含不含窺視、本場去重、誰可呼叫、抽到動作／窺視怎麼落地）。  
3. 使用者確認後再進 **2. 閒置**。  
4. **未說「實作／execute」前不要改程式。**

---

## 一鍵開場白（可貼到新對話）

請讀 `HS2OrbitAndExciter/HANDOFF_fsm_state_redefine.md` 與  
`C:\Users\jason\.cursor\plans\fsm_狀態圖確認_bd3a5656.plan.md`。  
總則八點已定：未開 Ctrl+Shift+O 不介入；換段只認選池並隔離原版女方主導自動挑姿；自慰＝動作線；換角換衣僅手動；安靜不擋換段。  
請從 **1. 選池** 六欄契約草案開始，問題要講清楚（狀況／選項／建議）。
